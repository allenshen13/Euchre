// Project UID 1d9f47bfc76643019cfbf037641defe1

#include "Card.h"
#include <array>
#include <string>
#include "Pack.h"

Pack::Pack() {
    assert(false);
}

Pack::Pack(std::istream& pack_input) {
    assert(false);
    assert(next);
}

Card Pack::deal_one() {
    assert(false);
    assert(next);
}

void Pack::reset() {
    assert(false);
    assert(next);
}

void Pack::shuffle() {
    assert(false);
    assert(next);
}

bool Pack::empty() const {
    assert(false);
    assert(next);
}
